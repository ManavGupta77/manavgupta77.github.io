[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "trading_infrastructure"
version = "0.1.0"
description = "Algorithmic trading system for equities, futures, and options"
requires-python = ">=3.10"
